bundle:
  name: w3
variables:
  catalog:
    description: Target catalog
    default: workspace
  schema:
    description: Target schema
    default: default
resources:
  jobs:
    W3_job:
      name: W3_job
      tasks:
      - task_key: W3
        notebook_task:
          notebook_path: ./src/W3.py
targets:
  default:
    mode: development
    workspace:
      host: https://dbc-922a9e09-b3e2.cloud.databricks.com
    variables:
      catalog: workspace
      schema: default
