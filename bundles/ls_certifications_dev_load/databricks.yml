bundle:
  name: ls_certifications_dev_load
variables:
  catalog:
    description: Target catalog
    default: workspace
  schema:
    description: Target schema
    default: default
resources:
  jobs:
    LS_Certifications_DEV_Load_job:
      name: LS_Certifications_DEV_Load_job
      tasks:
      - task_key: LS_Certifications_DEV_Load
        notebook_task:
          notebook_path: ./src/LS_Certifications_DEV_Load.py
targets:
  default:
    mode: development
    workspace:
      host: https://dbc-922a9e09-b3e2.cloud.databricks.com
    variables:
      catalog: workspace
      schema: default
