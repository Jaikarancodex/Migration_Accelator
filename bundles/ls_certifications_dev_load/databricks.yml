bundle:
  name: ls_certifications_dev_load
variables:
  catalog:
    description: Target catalog
    default: migration_accelator
  schema:
    description: Target schema
    default: testing
resources:
  pipelines:
    LS_Certifications_DEV_Load_pipeline:
      name: LS_Certifications_DEV_Load_pipeline
      catalog: migration_accelator
      schema: testing
      serverless: true
      libraries:
      - file:
          path: ./src/LS_Certifications_DEV_Load.py
      - file:
          path: ./src/ls_certifications_dev_load_utils.py
targets:
  default:
    mode: development
    workspace:
      host: https://dbc-7e2fd23b-42d1.cloud.databricks.com/
    variables:
      catalog: migration_accelator
      schema: testing
